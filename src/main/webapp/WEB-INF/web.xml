<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                             http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <display-name>Spring Thymeleaf Hibernate Demo</display-name>

    <!-- ================== RequestId Filter（放最前面） ================== -->
    <!-- 為每次請求產生 UUID 並放入 MDC(requestId)，logback 可用 %X{requestId} 印出 -->
    <filter>
        <filter-name>requestIdFilter</filter-name>
        <filter-class>yingshop.common.logging.RequestIdFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>requestIdFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
    <!-- ================== RequestId Filter（END） ================== -->

    <!-- ================== Spring Root Context ================== -->
    <context-param>
        <param-name>contextClass</param-name>
        <param-value>org.springframework.web.context.support.AnnotationConfigWebApplicationContext</param-value>
    </context-param>
    <context-param>
        <!-- 這裡是你的 Root 設定類的 package（保持你原本的設定） -->
        <param-name>contextConfigLocation</param-name>
        <param-value>com.example.demo.config</param-value>
    </context-param>

    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>
    <!-- ================== Spring Root Context（END） ================== -->

    <!-- ================== Spring MVC DispatcherServlet ================== -->
    <servlet>
        <servlet-name>dispatcher</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextClass</param-name>
            <param-value>org.springframework.web.context.support.AnnotationConfigWebApplicationContext</param-value>
        </init-param>
        <init-param>
            <!-- 這裡是 MVC 設定類（保持你原本的 WebMvcConfig） -->
            <param-name>contextConfigLocation</param-name>
            <param-value>com.example.demo.config.WebMvcConfig</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>dispatcher</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>
    <!-- ================== DispatcherServlet（END） ================== -->

    <!-- ================== Character Encoding Filter ================== -->
    <filter>
        <filter-name>characterEncodingFilter</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>

    <filter-mapping>
        <filter-name>characterEncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
    <!-- ================== Character Encoding Filter（END） ================== -->

</web-app>
